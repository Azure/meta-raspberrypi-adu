[Unit]
Description=Create ADU diagnostics directory on boot partition
DefaultDependencies=no
After=local-fs.target
Before=sysinit.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'mkdir -p /boot/adu-diags && chmod 777 /boot/adu-diags'
RemainAfterExit=yes

[Install]
WantedBy=sysinit.target
